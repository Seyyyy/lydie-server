import { HomePage } from "./HomePage";

const Template = () => {
  return <HomePage />;
};

export default {
  title: "Page/HomePage",
  component: HomePage,
};

export const Prototype = () => <Template />;
